import React from "react";
import './Navbar.css';

const Navbar = () => {
    return(
        <div>
            <nav className="heading-box">
                <h1 className="heading">Top Courses</h1>
            </nav>
        </div>
    );
};

export default Navbar;